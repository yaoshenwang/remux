# Native Docs

This directory is reserved for active desktop and mobile client documentation when those surfaces become current implementation work.

Archive-era native plans should stay under `docs/archive/` until they are reactivated by current code and ADRs.
